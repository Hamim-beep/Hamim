#include <stdio.h>

int main()
{
    int x, y, sum;

    scanf("%d%d", &x, &y);

    printf("The sum of integers:\n");

    sum = x + y;

    printf("Sum = %d", sum);

    return 0;
}
