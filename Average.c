#include <stdio.h>

int main() {
    int n;
    float average;

    printf("Enter n: ");
    scanf("%d", &n);

    average = (n + 1) / 2;

    printf("Average = %f\n", average);

    return 0;
}
